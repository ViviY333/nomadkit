    .now-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-bottom: 4px; }
    .now-card {
      min-height: 112px;
      padding: 13px 12px;
      border-radius: 21px;
      border: 1px solid rgba(40, 34, 25, .11);
      box-shadow: 0 12px 26px rgba(40, 34, 25, .055);
      display: flex;
      flex-direction: column;
    }
    .now-card .now-label { font-size: 10px; letter-spacing: .12em; text-transform: uppercase; font-weight: 900; color: var(--faint); }
    .now-card .now-emoji { margin-top: 8px; font-size: 24px; line-height: 1; }
    .now-card .now-main { margin-top: auto; font-family: Georgia, serif; font-size: 30px; line-height: 1; letter-spacing: -.04em; }
    .now-card .now-main .u { margin-left: 2px; font-size: 14px; font-family: var(--body); }
    .now-card .now-main2 { margin-top: auto; font-family: var(--display); font-size: 17px; line-height: 1.12; letter-spacing: -.03em; }
    .now-card .now-sub { margin-top: 5px; color: var(--muted); font-size: 11px; line-height: 1.3; }
    .now-card.w-weather { background: linear-gradient(160deg, rgba(39, 91, 115, .14), rgba(255, 248, 235, .72)); }
    .now-card.w-temp { background: linear-gradient(160deg, rgba(189, 88, 53, .14), rgba(255, 248, 235, .72)); }
    .now-card.w-spot { background: linear-gradient(160deg, rgba(104, 118, 80, .16), rgba(255, 248, 235, .72)); }

